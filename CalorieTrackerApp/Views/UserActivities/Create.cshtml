@model CalorieTrackerApp.Models.UserActivity

<h2>Add Activity</h2>

<form asp-action="Create" method="post">
    <div asp-validation-summary="All" class="text-danger"></div>
    @Html.AntiForgeryToken()
    <div class="form-group mb-2">
        <label asp-for="Name"></label>
        <input asp-for="Name" class="form-control" />
    </div>

    <div class="form-group mb-2">
        <label asp-for="CaloriesBurned"></label>
        <input asp-for="CaloriesBurned" class="form-control" />
    </div>

    <div class="form-group mb-2">
        <label asp-for="UserProfileId">User</label>
        <select asp-for="UserProfileId" asp-items="ViewBag.UserProfileId" class="form-control">
            <option value="">Select User</option>
        </select>
    </div>

    <button type="submit" class="btn btn-success mt-2">Save</button>
    <a asp-action="Index" class="btn btn-danger mt-2">Cancel</a>

</form>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
@model IEnumerable<CalorieCore.DataModels.Recipe>
@{
    var globalRecipes = ViewBag.GlobalRecipes as List<CalorieCore.DataModels.Recipe> ?? new List<CalorieCore.DataModels.Recipe>();
}

@Html.AntiForgeryToken()

<div class="container mt-4">
    @* --- TOP PART: TITLE & TOGGLE --- *@
    <div class="d-flex align-items-center justify-content-between mb-3">
        <div class="d-flex align-items-center gap-2">
            <h3 class="gradient-text fw-bold mb-0">Global Discovery</h3>
            <img src="~/images/Food.png" alt="Recipes" style="height:32px;">
        </div>
        <div class="d-flex gap-2">
            <button class="btn btn-outline-warning btn-sm fw-bold shadow-sm" type="button" data-bs-toggle="offcanvas" data-bs-target="#myCollectionSidebar">
                <i class="bi bi-heart-fill me-1 text-danger"></i> My Collection
            </button>
            <a asp-action="Create" class="btn btn-warning btn-sm fw-bold shadow-sm">
                <i class="bi bi-plus-lg"></i> New Recipe
            </a>
        </div>
    </div>

    @* --- SEARCH & FILTERS --- *@
    <div class="glass-card p-2 mb-4 shadow mx-auto" style="max-width: 1000px; position: relative; z-index: 1100;">
        <div class="row g-2">
            <div class="col-md-8">
                <div class="input-group input-group-sm">
                    <span class="input-group-text bg-transparent border-secondary text-secondary">
                        <i class="bi bi-search"></i>
                    </span>
                    <input type="text" id="recipeSearch" class="form-control bg-transparent text-white border-secondary"
                           placeholder="Search recipes, ingredients, tags...">
                </div>
            </div>
            <div class="col-md-4">
                <div class="dropdown w-100">
                    <button class="btn btn-outline-light-custom btn-sm w-100 dropdown-toggle fw-bold" type="button" id="filterBtn" data-bs-toggle="dropdown">
                        <i class="bi bi-funnel"></i> Filters
                    </button>
                    <div class="dropdown-menu dropdown-menu-dark dropdown-menu-end p-3 shadow-lg border-secondary" style="width: 300px; z-index: 9999 !important;">
                        <label class="small text-secondary fw-bold mb-1">Type of Food</label>
                        <select class="form-select form-select-sm mb-2 filter-trigger" id="filterType">
                            <option value="">All Types</option>
                            <option>Breakfast</option>
                            <option>Main</option>
                            <option>Salad</option>
                            <option>Soup</option>
                            <option>Snack</option>
                            <option>Dessert</option>
                        </select>
                        <label class="small text-secondary fw-bold mb-1">Difficulty</label>
                        <select class="form-select form-select-sm mb-2 filter-trigger" id="filterDiff">
                            <option value="">All Difficulties</option>
                            <option>Easy</option>
                            <option>Medium</option>
                            <option>Hard</option>
                        </select>
                        <label class="small text-secondary fw-bold mb-2">Sort by Calories</label>
                        <select class="form-select form-select-sm mb-3 filter-trigger" id="sortCal">
                            <option value="none">Default</option>
                            <option value="low">Lowest First</option>
                            <option value="high">Highest First</option>
                        </select>
                        <button class="btn btn-warning btn-sm w-100 fw-bold" onclick="resetFilters()">Reset All</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    @* --- GLOBAL GALLERY --- *@
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 row-cols-xl-4 g-3 compact-grid" id="globalRecipesContainer">
        @foreach (var item in globalRecipes)
        {
            <div class="col recipe-card-wrapper"
                 data-id="@item.Id"
                 data-title="@item.Title"
                 data-type="@item.Type"
                 data-difficulty="@item.Difficulty"
                 data-calories="@item.Calories">
                <partial name="_RecipeCard" model="item" />
            </div>
        }
    </div>

    @* --- GLOBAL PAGINATION --- *@
    <nav class="mt-5">
        <ul class="pagination pagination-sm justify-content-center">
            @for (int i = 1; i <= ViewBag.GlobalTotalPages; i++)
            {
                <li class="page-item @(i == ViewBag.GlobalCurrentPage ? "active" : "")">
                    <a class="page-link bg-dark text-light border-secondary px-3"
                       href="@Url.Action("Index", new { globalPage = i, searchString = ViewBag.SearchString })">@i</a>
                </li>
            }
        </ul>
    </nav>
</div>

@* --- MY COLLECTION SIDEBAR --- *@
<div class="offcanvas offcanvas-end bg-dark text-white border-start border-secondary" tabindex="-1" id="myCollectionSidebar" style="width: 380px;">
    <div class="offcanvas-header border-bottom border-secondary">
        <h5 class="offcanvas-title fw-bold text-warning"><i class="bi bi-heart-fill me-2 text-danger"></i>My Collection</h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="offcanvas"></button>
    </div>
    <div class="offcanvas-body">
        <div id="myRecipesSidebarContainer" class="compact-grid d-flex flex-column gap-3">
            @foreach (var item in Model)
            {
                <div class="sidebar-item recipe-card-wrapper" data-id="@item.Id">
                    <partial name="_RecipeCard" model="item" />
                </div>
            }
        </div>
    </div>
</div>

<div class="modal fade" id="deleteRecipeModal" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered">
        <div id="deleteModalContent"></div>
    </div>
</div>

@section Scripts {
    <script>
        const searchInput = document.getElementById("recipeSearch");
        const triggers = document.querySelectorAll(".filter-trigger");

        function applyFilters() {
            const search = searchInput.value.toLowerCase();
            const type = document.getElementById("filterType").value.toLowerCase();
            const diff = document.getElementById("filterDiff").value.toLowerCase();
            const sort = document.getElementById("sortCal").value;

            const cards = document.querySelectorAll("#globalRecipesContainer .recipe-card-wrapper");

            cards.forEach(card => {
                const cTitle = card.dataset.title.toLowerCase();
                const cType = card.dataset.type.toLowerCase();
                const cDiff = card.dataset.difficulty.toLowerCase();

                let isVisible = cTitle.includes(search);
                if (type && cType !== type) isVisible = false;
                if (diff && cDiff !== diff) isVisible = false;

                card.style.display = isVisible ? "block" : "none";
            });

            if (sort !== "none") {
                const container = document.getElementById('globalRecipesContainer');
                const items = Array.from(container.children);
                items.sort((a, b) => {
                    const calA = parseInt(a.dataset.calories);
                    const calB = parseInt(b.dataset.calories);
                    return sort === "low" ? calA - calB : calB - calA;
                });
                items.forEach(item => container.appendChild(item));
            }
        }

        async function toggleFavorite(recipeId, buttonElement) {
            const sidebarContainer = document.getElementById('myRecipesSidebarContainer');
            const allMatchingCards = document.querySelectorAll(`.recipe-card-wrapper[data-id="${recipeId}"]`);

            try {
                const response = await fetch(`/Recipes/ToggleFavorite/${recipeId}`, {
                    method: 'POST',
                    headers: { 'RequestVerificationToken': document.querySelector('input[name="__RequestVerificationToken"]').value }
                });

                if (response.ok) {
                    const data = await response.json();
                    if (data.success) {
                        const isNowFavorited = buttonElement.querySelector('i').classList.contains('bi-heart');

                        allMatchingCards.forEach(card => {
                            const icon = card.querySelector('.btn-outline-danger i, .btn-link i');
                            if (icon) {
                                if (isNowFavorited) {
                                    icon.classList.replace('bi-heart', 'bi-heart-fill');
                                    icon.classList.replace('text-white', 'text-danger');
                                } else {
                                    icon.classList.replace('bi-heart-fill', 'bi-heart');
                                    icon.classList.replace('text-danger', 'text-white');
                                }
                            }
                        });

                        if (isNowFavorited) {
                            if (!sidebarContainer.querySelector(`.sidebar-item[data-id="${recipeId}"]`)) {
                                const globalCard = document.querySelector(`#globalRecipesContainer .recipe-card-wrapper[data-id="${recipeId}"]`);
                                if (globalCard) {
                                    const clone = globalCard.cloneNode(true);
                                    clone.classList.remove('col');
                                    const sidebarItem = document.createElement('div');
                                    sidebarItem.className = 'sidebar-item recipe-card-wrapper';
                                    sidebarItem.setAttribute('data-id', recipeId);
                                    sidebarItem.appendChild(clone.firstElementChild); 
                                    sidebarContainer.prepend(sidebarItem);
                                }
                            }
                        } else {
                            const match = sidebarContainer.querySelector(`.sidebar-item[data-id="${recipeId}"]`);
                            if (match) match.remove();
                        }
                    }
                }
            } catch (e) { console.error("Toggle Favorite Error:", e); }
        }

        function resetFilters() {
            searchInput.value = "";
            triggers.forEach(t => t.value = t.id === 'sortCal' ? 'none' : '');
            applyFilters();
        }

        searchInput.addEventListener("keyup", applyFilters);
        triggers.forEach(t => t.addEventListener("change", applyFilters));

        function openDeleteModal(id) {
            $.get("/Recipes/Delete/" + id, function(data) {
                $('#deleteModalContent').html(data);
                new bootstrap.Modal(document.getElementById('deleteRecipeModal')).show();
            });
        }
    </script>

    <style>
        .recipe-card-wrapper {
            display: flex;
        }

            .recipe-card-wrapper .card {
                width: 100%;
                display: flex;
                flex-direction: column;
                border-radius: 8px !important;
                height: 100%;
            }

        .compact-grid .card-body {
            padding: 0.75rem !important;
            display: flex;
            flex-direction: column;
            flex-grow: 1;
        }

        .compact-grid .card-title {
            font-size: 0.9rem !important;
            margin-bottom: 0.2rem !important;
            line-height: 1.2;
            height: 2.4rem;
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
        }

        .compact-grid .card-text {
            height: 3rem;
            font-size: 0.75rem !important;
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            margin-bottom: 1rem;
        }

        .mt-auto {
            margin-top: auto !important;
        }

        .compact-grid .badge {
            font-size: 0.6rem !important;
            padding: 0.2rem 0.4rem !important;
        }

        .compact-grid .btn {
            padding: 0.2rem 0.4rem !important;
            font-size: 0.7rem !important;
        }

        .offcanvas {
            box-shadow: -5px 0 25px rgba(0,0,0,0.5);
        }

        .recipe-card-wrapper:hover {
            transform: translateY(-3px);
            transition: 0.2s;
        }

        .bg-custom-tag {
            background-color: #BDE3FF !important;
            color: #002B4D !important;
            font-weight: 600;
        }

        .pagination .page-link:hover, .pagination .active .page-link {
            background-color: #ffc107 !important;
            border-color: #ffc107 !important;
            color: #000 !important;
        }
        .offcanvas.offcanvas-end {
            transition: transform 0.4s cubic-bezier(0.25, 1, 0.5, 1) !important;
        }

        #myRecipesSidebarContainer .sidebar-item {
            opacity: 0;
            transform: translateX(20px);
            transition: all 0.4s ease-out;
        }

        .offcanvas.show #myRecipesSidebarContainer .sidebar-item {
            opacity: 1;
            transform: translateX(0);
        }
            .offcanvas.show #myRecipesSidebarContainer .sidebar-item:nth-child(1) {
                transition-delay: 0.1s;
            }

            .offcanvas.show #myRecipesSidebarContainer .sidebar-item:nth-child(2) {
                transition-delay: 0.15s;
            }

            .offcanvas.show #myRecipesSidebarContainer .sidebar-item:nth-child(3) {
                transition-delay: 0.2s;
            }

            .offcanvas.show #myRecipesSidebarContainer .sidebar-item:nth-child(4) {
                transition-delay: 0.25s;
            }
    </style>
}